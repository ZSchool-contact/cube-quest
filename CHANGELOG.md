# יומן גרסאות — «מסע הקוביות»

הקישור החי: <https://zschool-contact.github.io/cube-quest/>

הריפו הזה מכיל **רק את המשחק המוגש** (`index.html`, קובץ עצמאי אחד). קוד המקור,
מפת הנושאים, מאגר השאלות וסקריפטי הבנייה נשארים מקומיים בפרויקט Master Evaluator.

שרשרת הפרסום: `build_game.py` (מ-`Question Bank\`) → `publish.py` (מ-`Game\`) →
commit ו-push כאן. GitHub Pages בונה תוך ~40 שניות.

---

## 6 באוגוסט 2026 (2) — מסך הכניסה אומר מה הוא עושה

**מה שמשתמש רואה**

- **מסך אחד בכל רגע: «יצירת חשבון להורה» או «כניסה לחשבון קיים».** קודם היו שני
  כפתורים שווים מתחת לאותו טופס, בלי כותרת ובלי שום סימן מי מהם פעיל — ולכן לא
  היה אפשר לדעת אם נוצר חשבון או לא. עכשיו יש כותרת, כפתור אחד מודגש, ומעבר מפורש
  בין המצבים («כבר יש לכם חשבון?»).
- **הודעות שאומרות מה לעשות.** כל כישלון הוצג קודם כמשפט אחד — «לבדוק
  אימייל/סיסמה (לפחות 6 תווים)» — גם כשהסיסמה הייתה תקינה לגמרי. עכשיו מוצגת
  הסיבה האמיתית: מייל שעדיין לא אושר · אימייל שכבר רשום · סיסמה שגויה · מכסת
  מיילים שנגמרה · כתובת לא תקינה.
- **הודעה טובה כבר לא צבועה באדום.** «החשבון נוצר, שלחנו מייל אישור» הופיע קודם
  באותו אדום של שגיאה, ולכן נקרא ככישלון.
- **אימייל שכבר רשום נאמר במפורש.** קודם הוצג «בדקו את המייל» על מייל שלא נשלח
  מעולם, וההורה חיכה לו לנצח. עכשיו המסך עובר למצב כניסה ואומר שהחשבון קיים.
- **«שכחתי סיסמה» ישירות במשחק**, כולל הטופס עצמו: הקישור מהמייל מחזיר למשחק,
  ושם בוחרים סיסמה חדשה. פתיחת הקישור לבדה אינה מחליפה סיסמה — עכשיו זה כתוב על
  המסך («עד שתשמרו אותה כאן — הסיסמה לא באמת השתנתה»).

**נבדק לפני העלייה**: קובץ בדיקה חדש (`test_auth_ui.py`) מריץ את המסך בדפדפן —
מצבים, מעבר ביניהם, בדיקות קלט, ופנייה אמיתית לשרת עם פרטים שגויים כדי לאמת
שההודעה המעורפלת לא חוזרת. `test_play` ו-`test_bidi` עוברים; אין שגיאות דפדפן.

---

## 6 באוגוסט 2026 — הדוח עובר להורה בלבד

**מה שמשתמש רואה**

- **הילד שמשחק כבר לא רואה שום פירוט בסיום.** מסך הניצחון האחרון לא מציג יותר
  כפתור «דוח מיפוי» — לא ציונים, לא סטטוס פר-נושא, לא רשימת שגיאות תפיסתיות.
  המשחק פשוט נגמר בברכה, בלי מספרים.
- **ההורה (המחובר) מקבל דוח מלא בנפרד**, דרך כפתור חדש «📊 הדוחות שלי» בתיבת
  ההתחברות — לא ברגע שהילד מסיים לשחק, אלא בכל זמן שההורה בוחר להיכנס ולבדוק.
  מציג את הדוח האחרון שנשמר לאותו חשבון.

**מה שמשתנה מתחת לפני השטח**

- הדוח נשמר לענן (Supabase) **ברגע שהמיפוי האמיתי נגמר** — לא מחכה עוד לעולם
  הבונוס, כדי שדוח לא ילך לאיבוד אצל ילד שמפסיק לשחק לפני שהעולם החופשי נגמר.
  שני ניסיונות שליחה (עם השהיה ביניהם) כדי שכשל רשת חד-פעמי לא ימחק דוח.
- הרשאות הקריאה על טבלת הדוחות עודכנו: הורה קורא את הדוחות **של עצמו** בלבד;
  מנהלת פדגוגית ממשיכה לקרוא הכל, לצורך שיבוץ לקבוצות.

**נבדק לפני העלייה**: כל 11 קובצי הבדיקה עברו נקי מול הבנייה המקומית; אין
שגיאות דפדפן.

---

## 5 באוגוסט 2026 — מדרגת אתגר, תחומי ליבה, והוראה אחידה

**מה שמשתמש רואה**

- **שאלת בונוס ⭐ מעל תקרת הכיתה.** עד עכשיו תלמיד שענה נכון על כל מדרגות כיתתו
  פשוט סיים את העולם, ורמתו האמיתית לא נמדדה — ילד כיתה ה׳ ברמת ז׳ נראה זהה לילד
  כיתה ה׳ ברמת ה׳. עכשיו הוא מקבל **שאלה אחת** מרמת הכניסה של הכיתה שמעליו,
  מסומנת «⭐ שאלת בונוס» ובלי תווית כיתה. הצלחה מופיעה במסך הסיום («ענית נכון גם
  על שאלת הבונוס — מעל מה שנלמד בכיתה שלך!»). **טעות אינה עולה דבר** — לא מורידה
  מדרגה, לא נחשבת ככישלון, והקובייה ניתנת בכל מקרה.
- **הוראה אחידה בשאלות «המשפט הנכון».** חמש שאלות בעברית שבהן המשימה היא לזהות
  את המשפט התקין קיבלו הוראה זהה — **«מהו המשפט הנכון?»**. קודם ההוראה הסגירה
  במה מדובר («בחרו את המשפט שבו יש התאמה נכונה במין ובמספר»), ולכן נבדקה הפעלת
  כלל ידוע ולא זיהוי השגיאה.

**מה שמשתנה מתחת לפני השטח**

- כל שאלה נושאת עכשיו **תחום ליבה** (`area`), וכל תשובה בקובץ המיפוי המיוצא
  נושאת אותו. זה מה שמאפשר לדוח למלווה לדבר בשם התחום — «כפל וחילוק», «שברים»,
  «תחביר» — במקום במספר מדרגה, ולהיקרא בלי מאגר השאלות לצידו.

**כללי מדרגת האתגר** (למי שקורא את הפלט)

| מצב | `end_reason` | השפעה על הרמה |
|---|---|---|
| עבר את הבונוס | `above_ceiling` + `above_ceiling:true` | **אין** — הרמה נשארת בתקרת הכיתה |
| טעה או דילג | `ceiling` | **אין** |
| כיתה ט׳ (אין מדרגה מעל) | `ceiling` | ללא שינוי מהגרסה הקודמת |

הבונוס הוא **דגל מצוינות ולא רמה**: תצפית יחידה אינה מדידה, ולכן הוודאות נשארת
`medium` בשני המקרים. הוא גם מוחרג ממוני הראיות, כדי שכישלון בשאלת פרס לא יוריד
את הוודאות של כל השרשרת.

**⚠️ שינוי בכניסה למשחק.** הגרסה הזאת מוגשת עם **שער התחברות** — הורה נדרש
להירשם באימייל, לאשר את המייל, למלא שם ילד/ה ולסמן הסכמה, ורק אז נפתחת בחירת
הגיל. זה מה שמאפשר לשמור התקדמות ולהמשיך ממכשיר אחר. **מי שנכנס לקישור בלי
חשבון לא יכול לשחק.** נבדק מול השרת: ההרשמה פתוחה ואישור מייל נדרש.

**נבדק לפני העלייה**: הקובץ החי מכיל את מדרגת האתגר, את חמש ההוראות המתוקנות ואת
שדה התחום; אין שגיאות דפדפן; אין גלילה אופקית ברוחב 390px.

---

## גרסאות קודמות

- **מהימנות הדוח ופלט השיבוץ לקבוצות** — `placement` בקובץ המיוצא, `end_reason`
  לכל שרשרת, דרגת ודאות, וסימון ילד שנמצא מתחת למדרגה הראשונה.
- **תיקוני תצוגה ל-RTL** — תרגיל מתמטי מקבל שורה משלו ולעולם לא נשבר בין שתי
  שורות; סימני `<` ו-`>` מבודדים כדי שלא ייצוירו הפוך.
- **8 עולמות ושלושה מקצועות** — עברית → מתמטיקה → אנגלית, שמירה והמשך בין
  עולמות, ועולם בונוס בלי שאלות.

<!-- flowpad:capsule identity
version: 1
data:
  id: 1d92189f-1c26-4961-baa7-a7c794d2285f
flowpad:endcapsule identity -->
